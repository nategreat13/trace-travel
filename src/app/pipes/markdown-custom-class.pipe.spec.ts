import { MarkdownCustomClassPipe } from './markdown-custom-class.pipe';

describe('MarkdownCustomClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownCustomClassPipe();
    expect(pipe).toBeTruthy();
  });
});
